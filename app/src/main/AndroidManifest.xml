<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="app.fifxiii">

    <!-- permissão para acessar a internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- permissão para acessar o estdo da conexao com a internet -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- permissao para acessar ligação -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_ff"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">

        <!-- MENU -->
        <activity
            android:name=".Menu"
            android:screenOrientation="sensorPortrait" />

        <!-- HOME -->
        <activity
            android:name=".Activity.MainActivity"
            android:launchMode="singleTask"
            android:screenOrientation="sensorPortrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AGENDA -->
        <activity
            android:name=".Activity.ScheduleActivity"
            android:launchMode="singleTask"
            android:screenOrientation="sensorPortrait" />

        <!-- INFO -->
        <activity
            android:name=".Activity.InfoActivity"
            android:launchMode="singleTask"
            android:screenOrientation="sensorPortrait" />

        <!-- LIST GROUP -->
        <activity
            android:name=".Activity.ListGroupActivity"
            android:launchMode="singleTask"
            android:screenOrientation="sensorPortrait" />

        <!-- DEV -->
        <activity
            android:name=".Activity.DevActivity"
            android:launchMode="singleTask"
            android:screenOrientation="sensorPortrait" />

        <!-- AOFF -->
        <activity
            android:name=".Activity.AofActivity"
            android:launchMode="singleTask"
            android:screenOrientation="sensorPortrait" />
        <activity android:name=".Activity.GroupActivity"></activity>
    </application>

</manifest>